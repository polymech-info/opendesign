/** Browser build. Designs live in OPFS; the API server is not running. */
export const webClient = true;

export const features = {
  chat: false,
  icons: false,
  appShot: false,
  projectExport: false,
};
